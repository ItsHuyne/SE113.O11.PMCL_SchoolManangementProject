using Đồ_án.Command;
using Đồ_án.ViewModel;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.CodeDom.Compiler;

namespace UnitTestProject.test.commands
{
    [TestClass]
    public class RelayCommandTest
    {
        [TestMethod]
        public void RelayCommandTest_ctor()
        {

            //Predicate<int> predicate = (t) => 1 == 1;
            //Action<int> action = (t) => { };
            //var profileCommand = new RelayCommand<int>(predicate, action);
            //Assert.IsNotNull(profileCommand);
        }
    }
}
